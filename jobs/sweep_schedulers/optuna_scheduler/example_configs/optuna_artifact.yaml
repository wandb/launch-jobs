# optuna_artifact.yaml
scheduler:
  # TODO(gst): make public wandb entity Optuna Scheduler job
  job: griffin_wb/public/job-wandb_job_sweep_scheduler_optuna:latest
  resource: local-container
  num_workers: 2
  settings:
    optuna_source: optuna_wandb.py

description: An Optuna scheduler using parameters from a pythonic search space
job: griffin_wb/public/job-source-public-train.py:latest
run_cap: 5

metric:
  name: val_acc
  goal: maximize
